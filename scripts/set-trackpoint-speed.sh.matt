# thinkpad trackpoint speed set
# The last value is the speed, from 0 to 1; the lower the value, the faster the speed.
# (This script is meant to run on startup with cli arg)
xinput --set-prop 12 "Coordinate Transformation Matrix" 1 0 0 0 1 0 0 0 $1
